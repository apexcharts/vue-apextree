name: Publish to npm

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    if: startsWith(github.event.head_commit.message, 'release:')

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check commit message and extract version
        id: check
        env:
          # Passed as an environment variable, never interpolated into the
          # script. `${{ }}` is substituted textually before bash parses, so a
          # commit message containing a double quote breaks the script, and one
          # containing $(...) or backticks would RUN in this job, which holds the
          # npm publishing token. Merge commit messages include the PR title,
          # which any outside contributor controls. This is not theoretical: it
          # broke a run in apexcharts-js on 2026-07-27, and again here when a
          # release body quoted a TypeScript expression in backticks.
          MSG: ${{ github.event.head_commit.message }}
        run: |
          if echo "$MSG" | grep -qE '^release: [0-9]+\.[0-9]+\.[0-9]+'; then
            VERSION=$(echo "$MSG" | grep -oE '[0-9]+\.[0-9]+\.[0-9]+[^ ]*' | head -1)
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            if echo "$VERSION" | grep -qE '[-]'; then
              echo "tag=next" >> $GITHUB_OUTPUT
            else
              echo "tag=latest" >> $GITHUB_OUTPUT
            fi
          else
            echo "❌ Commit message must match 'release: X.Y.Z'"
            exit 1
          fi

      - name: Verify package.json version matches commit message
        run: |
          PKG_VERSION=$(node -p "require('./package.json').version")
          MSG_VERSION="${{ steps.check.outputs.version }}"
          if [ "$PKG_VERSION" != "$MSG_VERSION" ]; then
            echo "❌ package.json version ($PKG_VERSION) does not match commit message version ($MSG_VERSION)"
            exit 1
          fi
          echo "✅ Version $PKG_VERSION confirmed"

      # Must stay recent enough for the npm the next step installs. npm@12
      # requires node ^22.22.2 || ^24.15.0 || >=26.0.0, so node 20 now fails at
      # `npm install -g npm@latest` before anything is published.
      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      # npm@latest, not the bundled npm: trusted publishing (OIDC, no token)
      # needs npm >= 11.5.1.
      - run: npm install -g npm@latest
      - run: npm install
      - run: npm run build

      - name: Publish to npm
        run: npm publish --provenance --tag ${{ steps.check.outputs.tag }}

      - name: Create git tag
        run: |
          TAG="v${{ steps.check.outputs.version }}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Tag $TAG already exists, skipping"
          else
            git tag -a "$TAG" -m "Release $TAG"
            git push origin "$TAG"
          fi

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="v${{ steps.check.outputs.version }}"
          PREV_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")
          if [ -n "$PREV_TAG" ]; then
            NOTES=$(git log "$PREV_TAG..HEAD" --pretty=format:"- %s" --no-merges)
          else
            NOTES=$(git log --pretty=format:"- %s" --no-merges)
          fi
          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "Release $TAG already exists, skipping"
          else
            gh release create "$TAG" --notes "$NOTES" --title "$TAG"
          fi
